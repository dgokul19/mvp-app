export const defaultAlertState = {
    alertState : false,
    alertType : '',
    alertMessage : ''
};

export const modalState = {
    openState : false,
    data : {},
    modalType : ''
};

export const defaultEmployeeState = {
    data : [],
    error : false,
    errorMessage : ''
};